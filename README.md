# za-warudo
# Renders the "README.md" file as .html
aka, za warudo
## Adds a "Download PDF" button, using the "html2pdf" library as the exporter
aka, za warudo... ***to***
### Uses the first recognized title (h{n°}) as the page name and PDF name
+ don't quest... simple za warudo
  - don't quest... simple za warudo
* don't quest... simple za warudo
#### Uses "icon.png" as the page icon
[Generates anchors with the "anchors.js" library](#za-warudo)
